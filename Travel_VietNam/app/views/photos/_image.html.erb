<%unless @images.blank? %>
<%count =0 %>
<table>
<%id =1 %>
<%for image in @images %>
<%if count==0 %>
<tr>
    <%end %>
    <td id="#{id}">
        <div class="highslide-gallery">
            <a href="<%=image.url_big%>" class="highslide" onclick="return hs.expand(this)"><img src="<%=image.url_small%>" /></a>
            <div class="highslide-caption">
                <a href="#" onclick="hs.close(this);">Sentfriend</a>
                <%= link_to "Foward", :action => "index" %>
            </div>
        </div>
        <br/>
        <!--if @user_id == "619807631"
        link_to_remote "delete",:update=>"#{id}",:url => {:controller=>"test",:action => "delete_image",:id=>image.id}image.id
        end-->
    </td>
    <td style="display:none">
        <img src="<%=image.url_big%>"/>
    </td><%count +=1 %>
    <%if count ==4 %>
</tr><%count=0 %>
<%end %>
<%end %>
<%if count !=4 %>
</tr><%count =0 %>
<%end %>
<tr>
    <td colspan="4" align="right">
        <%=will_paginate @images, :class=>"digg_pagination",:style=>"text-align:right;font-family: 'lucida grande', tahoma, verdana, arial, sans-serif;font-size: 12.5px;" %>
    </td>
</tr>
</table>
<%end %>